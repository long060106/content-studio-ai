# Environment / secrets
.env

# Python
__pycache__/
*.pyc
*.pyo
venv/
.venv/
env/
*.egg-info/

# Generated output
output/

# OS / editor
.DS_Store
.vscode/

# Downloaded/stock media (the manifest is tracked, the files aren't)
assets/video/
assets/image/
assets/music/

# The curated b-roll library. Nearly a gigabyte of stock footage, and it is
# rebuildable: `python gather_broll.py` refills the folders from their
# searches. The folder structure is what carries the meaning, not the files.
assets/broll/

# Footage pruned out of the library. Kept on disk rather than deleted so a
# wrong call can be undone without re-downloading, but it is still footage and
# has no business in a repository.
assets/_pruned/

# Live share link - contains the access key. Never publish.
current_link.txt
cookies.txt
*.log
